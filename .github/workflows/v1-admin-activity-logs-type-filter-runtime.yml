name: V1 Admin Activity Logs Type Filter Runtime

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  activity-logs-type-filter-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Assert exact PR head
        shell: bash
        run: |
          set -euo pipefail
          echo "EXACT_HEAD=$(git rev-parse HEAD)"
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Locked dependency install
        run: npm ci --no-audit --no-fund
      - name: Production build
        run: npm run build
      - name: Existing ENV-04 automated/runtime acceptance
        shell: bash
        run: |
          set -euo pipefail
          npm run preview -- --host 127.0.0.1 --port 4271 --strictPort > /tmp/admin-env04-logs-type-preview.log 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true; wait "$pid" 2>/dev/null || true' EXIT
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:4271/admin/login >/dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4271/admin/login >/dev/null
          CYPRESS_ADMIN_BASE_URL=http://127.0.0.1:4271 npx cypress run --browser electron --spec cypress/e2e/runtime/env04-admin-startup.cy.js
      - name: Real activity logs type filter application runtime
        shell: bash
        run: |
          set -euo pipefail
          npm run preview -- --host 127.0.0.1 --port 4272 --strictPort > /tmp/admin-logs-type-preview.log 2>&1 &
          pid=$!
          cleanup() {
            kill "$pid" 2>/dev/null || true
            wait "$pid" 2>/dev/null || true
          }
          trap cleanup EXIT
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:4272/admin/logs >/dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:4272/admin/logs >/dev/null
          CYPRESS_ADMIN_BASE_URL=http://127.0.0.1:4272 npx cypress run --browser electron --spec cypress/e2e/runtime/activity-logs-type-filter-runtime.cy.js
          kill -0 "$pid"
          status=$(curl -sS -o /tmp/admin-logs-type-route.html -w '%{http_code}' http://127.0.0.1:4272/admin/logs)
          test "$status" = "200"
          grep -q '<div id="root"></div>' /tmp/admin-logs-type-route.html
          echo 'ADMIN_ACTIVITY_LOGS_TYPE_FILTER_RUNTIME_PASS=true'
          echo 'ADMIN_PROCESS_ALIVE=true'
          echo "ADMIN_LOGS_ROUTE_HTTP_STATUS=$status"
          cat /tmp/admin-logs-type-preview.log